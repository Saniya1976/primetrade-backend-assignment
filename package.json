{
    "name": "primetrade-backend-assignment",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "npm run build:backend && npm run build:frontend",
        "build:backend": "cd backend && npm install && npm run generate",
        "build:frontend": "cd frontend && npm install && npm run build",
        "start": "cd backend && npm run migrate && node src/index.js"
    },
    "engines": {
        "node": "20.x",
        "npm": "10.x"
    }
}